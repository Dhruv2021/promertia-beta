document.addEventListener("DOMContentLoaded", function () {
    // Add a delay to keep the loader visible for 3 seconds
    setTimeout(function () {
        // Hide the loader
        document.body.classList.add("loaded");


 }, 3000); // Adjust the delay as needed for the preloader

});
